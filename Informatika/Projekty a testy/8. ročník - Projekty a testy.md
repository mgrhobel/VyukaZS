# Projekty a testy – Informatika 8. ročník

> **Hodnocení:** Písemné testy zaměřené na čtení a psaní kódu, praktické projekty s databází a webem.  
> Testy: 45 min · Projekty: hodnoceny rubrikou · Kód musí fungovat.

---

## Písemný test č. 1 – Python pokročile (slovníky, soubory, OOP)
**Termín:** Po prosinci  
**Délka:** 45 minut  
**Max bodů:** 16 b

---

### Instrukce pro žáky
Pracuj samostatně. Nepoužívej počítač. Čas: 45 minut.  
Hodnocení: 16–14 b = 1, 13–11 b = 2, 10–8 b = 3, 7–5 b = 4, méně = 5.

---

### ČÁST A – Čtení kódu (8 b)

**1. (4 b)** Co vypíše tento kód? Napiš přesný výstup.

```python
body = {"Anna": 85, "Bořek": 62, "Cyril": 91, "Dana": 74}

for jmeno, b in body.items():
    if b >= 75:
        print(f"{jmeno}: PROŠEL ({b} b)")
    else:
        print(f"{jmeno}: neprošel")

print("Průměr:", sum(body.values()) / len(body))
```

**2. (4 b)** Popiš, co dělá tato funkce (aspoň 3 věty). Co vrátí při volání `spocitej("poznamky.txt")`?

```python
def spocitej(nazev_souboru):
    pocet = 0
    try:
        with open(nazev_souboru, "r", encoding="utf-8") as f:
            for radek in f:
                if radek.startswith("#"):
                    pocet += 1
    except FileNotFoundError:
        return -1
    return pocet
```

---

### ČÁST B – Psaní kódu (8 b)

**3. (4 b)** Napiš třídu `Kniha` s těmito vlastnostmi:
- Atributy: `nazev`, `autor`, `rok` (předány v konstruktoru)
- Třídní proměnná `pocet_knih` (zvýší se o 1 při každé nové instanci)
- Metoda `info()` vrací řetězec: `"Název (Autor, rok)"`

Napiš také 2 řádky kódu, které vytvoří 2 instance a vypíší `pocet_knih`.

**4. (4 b)** Napiš program, který:
- Načte jména ze souboru `"trida.txt"` (každé jméno na samostatném řádku)
- Uloží je do slovníku s počáteční hodnotou 0 (jméno → body)
- Umožní uživateli zadávat: `jméno body` (např. `Anna 5`)
- Po zadání `konec` vypíše všechna jména a jejich body

---

### 🔑 Klíč – Test č. 1

**1.**
```
Anna: PROŠEL (85 b)
Bořek: neprošel
Cyril: PROŠEL (91 b)
Dana: neprošel
Průměr: 78.0
```
**2.** Funkce otevře soubor a spočítá řádky začínající `#`. Při chybějícím souboru vrátí -1. Jinak vrátí počet komentářových řádků.  
**3.**
```python
class Kniha:
    pocet_knih = 0
    
    def __init__(self, nazev, autor, rok):
        self.nazev = nazev
        self.autor = autor
        self.rok = rok
        Kniha.pocet_knih += 1
    
    def info(self):
        return f"{self.nazev} ({self.autor}, {self.rok})"

k1 = Kniha("Harry Potter", "Rowling", 1997)
k2 = Kniha("Hobit", "Tolkien", 1937)
print(Kniha.pocet_knih)  # 2
```
**4.**
```python
zaci = {}
try:
    with open("trida.txt", "r", encoding="utf-8") as f:
        for radek in f:
            zaci[radek.strip()] = 0
except FileNotFoundError:
    print("Soubor nenalezen")

vstup = ""
while vstup != "konec":
    vstup = input("Jméno body (nebo 'konec'): ")
    if vstup != "konec":
        casti = vstup.split()
        if len(casti) == 2 and casti[0] in zaci:
            zaci[casti[0]] += int(casti[1])

for jmeno, body in zaci.items():
    print(f"{jmeno}: {body} b")
```

---

## Projekt č. 1 – Python aplikace s databází
**Termín odevzdání:** Po březnu  
**Délka práce:** 2 hodiny v hodině + domácí dopracování  
**Max bodů:** 10 b

---

### Zadání pro žáky

Vytvoř Python aplikaci, která pracuje s SQLite databází. Vyber si téma:

- **A) Adresář** – CRUD pro kontakty (jméno, telefon, e-mail)
- **B) Sklady/inventář** – sledování produktů (název, počet kusů, cena)
- **C) Deník hodnocení** – žáci, předměty, známky

Povinné požadavky:
- ✓ ER diagram (na papíře nebo draw.io)
- ✓ SQLite databáze s aspoň 1 tabulkou
- ✓ Funkce: přidat záznam, zobrazit záznamy, hledat/filtrovat, smazat
- ✓ Ošetření chyb (try/except)
- ✓ Komentáře v kódu

Odevzdej: `.py` soubor + ER diagram (foto nebo .png).

---

### Rubrika hodnocení

| Kritérium | Body |
|---|---|
| ER diagram (správný návrh tabulek a vztahů) | 3 |
| Funkční SQL dotazy (min. SELECT, INSERT, UPDATE nebo DELETE) | 3 |
| Python aplikace funguje (celý program, uživatelské rozhraní) | 4 |
| **Max** | **10** |

---

## Projekt č. 2 – Webová stránka s JavaScriptem
**Termín odevzdání:** Po dubnu  
**Délka práce:** 2 hodiny v hodině + domácí dopracování  
**Max bodů:** 10 b

---

### Zadání pro žáky

Vytvoř webovou stránku s interaktivitou v JavaScriptu. Vyber si téma:

- **A) Interaktivní kvíz** – min. 5 otázek, bodování, zpětná vazba po každé odpovědi
- **B) Kalkulátor** – základní operace + vědecké funkce (mocnina, druhá odmocnina)
- **C) Mini hra v prohlížeči** – jednoduché (klikni na objekt, odpočet, skóre)

Povinné požadavky:
- ✓ Validní HTML struktura
- ✓ Vlastní CSS (min. 5 pravidel, vlastní barevné schéma)
- ✓ JavaScript: aspoň 2 funkce, 1 event listener, manipulace s DOM
- ✓ Žádné knihovny (jen čistý HTML + CSS + JS)

---

### Rubrika hodnocení

| Kritérium | Body |
|---|---|
| Validní HTML struktura | 3 |
| Vlastní CSS (barevné schéma, čitelnost) | 3 |
| JavaScript funkčnost (interaktivita funguje) | 4 |
| **Max** | **10** |

---

## Projekt č. 3 – Fotografování a postprodukce
**Termín odevzdání:** Červen (závěr roku)  
**Délka práce:** H29 (2 hodiny) + domácí dokončení  
**Max bodů:** 8 b

---

### Zadání pro žáky

Vytvoř fotosérii na téma dle vlastního výběru a zpracuj ji v grafickém editoru.

**Povinné součásti:**

1. **Fotosoustava** – 3–5 fotografií na jedno téma (např. architektura školy, příroda, příběh v sérii)
   - Fotografie musí být vlastní (pořízené žákem)
   - Aspoň 2 fotky musí demonstrovat vědomé použití **pravidla třetin**

2. **Postprodukce** (v GIMP / Photopea / Canva):
   - Každá fotka musí projít aspoň 3 různými úpravami z: ořez, jas/kontrast, vyvážení barev, ostření, odstranění pozadí, barevný filtr
   - Zachová se originál i upravená verze

3. **Průvodní komentář** (150–200 slov):
   - Proč jsi zvolil/a toto téma?
   - Jaké nastavení foťáku / telefonu jsi použil/a? (expozice, zaostření, …)
   - Co se ti povedlo a co bys příště udělal/a jinak?

**Odevzdání:** Složka s originály, upravenými verzemi a komentářem (Word / PDF)

---

### Rubrika hodnocení

| Kritérium | Body |
|---|---|
| Kompozice – pravidlo třetin, světlo, záměr (hodnoceno 2 snímky) | 3 |
| Postprodukce – správné použití min. 3 různých nástrojů | 3 |
| Průvodní komentář – reflexe procesu a rozhodnutí | 2 |
| **Max** | **8** |

---

### 🔑 Hodnotící poznámky pro učitele

- Technická kvalita fotky (rozlišení, šum) není primárním kritériem – důležitý je **záměr a kompozice**
- Postprodukce: žáci by měli prokázat, že chápou proč danou úpravu provedli
- Pokud žák nemá přístup k telefonu/foťáku: lze domluvit zapůjčení školního zařízení nebo použít volné fotky z Unsplash.com (pak komentář o výběru a úpravách)

---

## Závěrečný test – celý ročník
**Termín:** Červen  
**Délka:** 45 minut  
**Max bodů:** 20 b

---

### Instrukce pro žáky
Pracuj samostatně. Nepoužívej počítač. Čas: 45 minut.

---

### ČÁST A – Python (8 b)

**1. (4 b)** Co vypíše tento kód? Pracuj krok za krokem.

```python
class Pocitadlo:
    celkem = 0
    
    def __init__(self, start=0):
        self.hodnota = start
        Pocitadlo.celkem += 1
    
    def pridej(self, n):
        self.hodnota += n
        return self
    
    def __str__(self):
        return f"Hodnota: {self.hodnota}"

a = Pocitadlo(10)
b = Pocitadlo()
a.pridej(5).pridej(3)
print(a)
print(b)
print("Počet instancí:", Pocitadlo.celkem)
```

**2. (4 b)** Napiš funkci `nacti_kontakty(soubor)`, která:
- Přečte CSV soubor (formát: `jmeno,telefon` na každém řádku)
- Vrátí slovník `{"jmeno": "telefon", ...}`
- Ošetří výjimku FileNotFoundError (vrátí prázdný slovník)

---

### ČÁST B – Databáze (6 b)

**3. (3 b)** Máme tabulky:
```
zaci(id, jmeno, trida)
hodnoceni(id, zak_id, predmet, znamka)
```

Napiš SQL dotaz, který vypíše **jméno a průměrnou známku** každého žáka z třídy `8A`, seřazené od nejlepšího (nejnižší průměr).

**4. (3 b)** Vysvětli, co je **SQL injection** a jak se před ní bránit. Ukaž na příkladu bezpečný a nebezpečný kód v Pythonu (sqlite3).

---

### ČÁST C – Web (6 b)

**5. (3 b)** Napiš JavaScript funkci `zkontrolujHeslo(heslo)`, která vrátí `true` pokud:
- Heslo má aspoň 8 znaků
- Heslo obsahuje aspoň jedno číslo

**6. (3 b)** Oprav a doplň tento JavaScript kód pro interaktivní kvíz:

```javascript
let skore = 0;

function zkontroluj(odpoved) {
    if (odpoved = "Praha") {  // chyba 1
        skore++;
        document.getElementId("zpetna-vazba").textContent = "Správně!";  // chyba 2
    } else {
        document.getElementById("zpetna-vazba").textContent = "Špatně.";
    }
}
```

---

### 🔑 Klíč – Závěrečný test

**1.**
```
Hodnota: 18
Hodnota: 0
Počet instancí: 2
```
**2.**
```python
def nacti_kontakty(soubor):
    kontakty = {}
    try:
        with open(soubor, "r", encoding="utf-8") as f:
            for radek in f:
                casti = radek.strip().split(",")
                if len(casti) == 2:
                    kontakty[casti[0]] = casti[1]
    except FileNotFoundError:
        return {}
    return kontakty
```
**3.**
```sql
SELECT z.jmeno, AVG(h.znamka) as prumer
FROM zaci z
JOIN hodnoceni h ON z.id = h.zak_id
WHERE z.trida = '8A'
GROUP BY z.id, z.jmeno
ORDER BY prumer ASC;
```
**4.** SQL injection = útočník vloží SQL kód do vstupu. Nebezpečné: `cursor.execute("SELECT * FROM users WHERE name = '" + vstup + "'")`. Bezpečné: parametrizovaný dotaz: `cursor.execute("SELECT * FROM users WHERE name = ?", (vstup,))`  
**5.**
```javascript
function zkontrolujHeslo(heslo) {
    if (heslo.length < 8) return false;
    return /\d/.test(heslo);
}
```
**6.** Chyba 1: `=` → `===` (porovnání). Chyba 2: `getElementId` → `getElementById`
